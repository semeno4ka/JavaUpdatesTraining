package com.cydeo.HomeTask;

import com.cydeo.farmerTask.Color;

public interface ApplePredicate {
     String print(Apple apple);// because we will use the particular feature of particular object(color, weight)
}
